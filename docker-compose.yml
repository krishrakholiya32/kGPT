version: "3.8"

services:
  app:
    build: .
    container_name: kgpt-app
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./database:/app/database
    restart: unless-stopped
