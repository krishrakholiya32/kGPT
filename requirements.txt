# ===== Core =====
fastapi
uvicorn[standard]
python-dotenv

# ===== LLM (raw HTTP) =====
# LangChain removed in favour of direct httpx calls to Groq + Gemini
# (see backend/agent/llm.py). httpx is pinned explicitly.
httpx>=0.27

# ===== Database (async Postgres) =====
sqlalchemy[asyncio]
asyncpg

# ===== Auth =====
pyjwt[crypto]
pwdlib[argon2]
python-multipart

# ===== Validation =====
pydantic[email]

# ===== Web search =====
ddgs
duckduckgo-search

# ===== File attachment extraction =====
pymupdf
python-docx
groq

# ===== RAG / cross-conversation memory (local embeddings + pgvector) =====
# Local sentence-transformers embeddings (not an API call) — verified on the
# actual Oracle ARM box: ~539MB resident, ~14ms/query, well within budget.
# CPU-only torch build (install with --extra-index-url
# https://download.pytorch.org/whl/cpu to avoid resolving a CUDA build on
# platforms where that matters; aarch64 Linux only publishes CPU wheels
# anyway, so this mainly matters for reproducibility on other architectures).
pgvector==0.5.0
sentence-transformers>=3.0
torch>=2.2
